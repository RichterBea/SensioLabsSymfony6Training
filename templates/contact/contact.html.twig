{% extends 'base.html.twig' %}

{% block title %}Hello MovieController!{% endblock %}

{% block body %}
    <h1>Hey! Here you can contact us</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit", class="btn btn-primary">Send</button>
    {{ form_end(form) }}
{% endblock %}
